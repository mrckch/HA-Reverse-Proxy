# /etc/nginx/conf.d/stub-status.conf
# Liefert Metriken für die Statusseite — nur lokal erreichbar

server {
    listen 127.0.0.1:8081;
    server_name localhost;

    location /nginx_status {
        stub_status;
        allow 127.0.0.1;
        deny all;
        access_log off;
    }
}
